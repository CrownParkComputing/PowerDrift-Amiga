# Power Drift ROMs

Place the MAME `pdrift` set here as either:

- `pdrift.zip`
- extracted ROM files directly in this directory

The source extractor also checks common local MAME paths, but the
project-local copy is preferred for repeatable builds.

ROM images are NOT included in this repository. You must supply your own
legally obtained MAME `pdrift` set.